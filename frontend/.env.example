# Local two-origin development (Vite :3000 + FastAPI :8000).
VITE_API_BASE_URL=http://127.0.0.1:8000

# Production (single origin: FastAPI serves frontend/dist + /api):
# leave VITE_API_BASE_URL unset/empty so the client uses same-origin /api.
